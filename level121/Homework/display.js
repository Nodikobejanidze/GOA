// display.js

// Default export import
const message = require('./message');

// Output the greeting text and language to the console
console.log(message.greeting);  // "Hello"
console.log(message.language);  // "English"
